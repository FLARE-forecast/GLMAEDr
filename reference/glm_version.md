# Return the GLM version string

Invokes the installed GLM executable with `-help` and returns the
captured output, which includes the version number.

## Usage

``` r
glm_version()
```

## Value

A character string with the GLM version information.

## Examples

``` r
if (FALSE) { # \dontrun{
glm_version()
} # }
```
