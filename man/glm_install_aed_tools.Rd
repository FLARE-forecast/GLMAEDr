% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{glm_install_aed_tools}
\alias{glm_install_aed_tools}
\title{Install GLM from the AED_Tools repository}
\usage{
glm_install_aed_tools(
  repo = aed_tools_default_repo,
  ref = "HEAD",
  private = FALSE,
  no_gui = TRUE,
  jobs = 1L,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{URL of the AED_Tools git repository. Ignored when \code{private = TRUE}.}

\item{ref}{Branch or tag to check out inside the \verb{GLM/} sub-repository
after fetching sources. Accepts:
\itemize{
\item \code{"HEAD"} (default) — whatever branch \code{fetch_sources.sh} leaves checked
out
\item a branch name, e.g. \code{"v4alpha"}
\item a tag, e.g. \code{"v4.0.0"}
\item a full or abbreviated commit SHA, e.g. \code{"a3f2c1d"}
}}

\item{private}{Logical. Clone from \code{AED_Tools_Private} instead of the
public \code{AED_Tools} repository. Requires that your git credentials
(SSH key or personal access token) have read access to
\code{github.com/AquaticEcoDynamics/AED_Tools_Private}. Default \code{FALSE}.}

\item{no_gui}{Logical. Build the headless version of GLM. Default \code{TRUE}.}

\item{jobs}{Integer. Number of parallel \code{make} jobs. Default \code{1L}.}

\item{force}{Logical. Reinstall even when a binary already exists.}

\item{quiet}{Logical. Suppress build output. Default \code{FALSE}.}
}
\value{
Invisibly returns the path to the installed GLM executable.
}
\description{
Clones the
\href{https://github.com/AquaticEcoDynamics/AED_Tools}{AED_Tools} repository
(or its private counterpart), fetches GLM source via \code{fetch_sources.sh},
optionally switches the GLM sub-repository to a specific branch or tag,
then compiles GLM with \code{build_glm.sh}. The resulting binary is stored in
the same per-user data directory as \code{glm_install()}.
}
\details{
\subsection{System requirements}{

Same as \code{glm_install()} — see \code{?glm_install} for details.
}
}
\examples{
\dontrun{
glm_install_aed_tools(ref = "v4alpha")
glm_install_aed_tools(private = TRUE, ref = "v4alpha")
}

}
