% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{glm_install}
\alias{glm_install}
\title{Install the GLM executable by compiling from source}
\usage{
glm_install(
  repo = glm_default_repo,
  ref = "HEAD",
  no_gui = TRUE,
  jobs = 1L,
  force = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{repo}{URL of any GLM-AED-style git repository. Forks and mirrors
are supported as long as they preserve the \code{glm-source/build_glm.sh}
structure.}

\item{ref}{What to check out after cloning. Accepts:
\itemize{
\item \code{"HEAD"} (default) -- tip of the default branch
\item a branch name, e.g. \code{"develop"}
\item a tag, e.g. \code{"v3.9.107"}
\item a full or abbreviated commit SHA, e.g. \code{"a3f2c1d"}
}}

\item{no_gui}{Logical. Build the headless version of GLM (no graphical
output window). Recommended for use from R. Default \code{TRUE}.}

\item{jobs}{Integer. Number of parallel \code{make} jobs. Defaults to \code{1L}.
Fortran module compilations have implicit ordering dependencies that
can race under parallel make; increase only if you have verified that
the upstream Makefiles declare explicit module dependencies.}

\item{force}{Logical. Reinstall even when a binary already exists.}

\item{quiet}{Logical. Suppress build output. Default \code{FALSE}.}
}
\value{
Invisibly returns the path to the installed GLM executable.
}
\description{
Downloads the GLM-AED source repository and compiles the GLM executable
for the current platform. The resulting binary is stored in a per-user
data directory so it survives package upgrades and requires no
administrator privileges. Only needs to be run once per machine (or after
a GLM version upgrade).
}
\details{
\subsection{System requirements}{

\strong{macOS} -- install via Homebrew:

\if{html}{\out{<div class="sourceCode">}}\preformatted{brew install gcc netcdf
}\if{html}{\out{</div>}}

\strong{Linux (Ubuntu/Debian)} -- install via apt:

\if{html}{\out{<div class="sourceCode">}}\preformatted{sudo apt-get install gfortran libnetcdf-dev debhelper
}\if{html}{\out{</div>}}

\strong{Windows} -- install
\href{https://cran.r-project.org/bin/windows/Rtools/}{RTools} for your R
version (provides \code{gfortran}, \code{make}, and NetCDF libraries).
}
}
\examples{
\dontrun{
glm_install()
}

}
